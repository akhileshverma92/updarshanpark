OOPS:-

in simple terms classes are just a way to define new sort of system reflecting real object in a program domain.


Inheritance is a mechanism that allows one class to inherit the properties and behavior of another class. The class that is being inherited is called the parent class or superclass, and the class that is doing the inheriting is called the child class or subclass.


